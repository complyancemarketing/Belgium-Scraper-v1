name: Daily E-Invoicing Scraper (All Countries)

on:
  schedule:
    # Runs daily at 10:00 PM UTC (adjust timezone as needed)
    # Format: minute hour day month day-of-week
    - cron: '0 22 * * *'
  
  # Allows manual trigger from GitHub Actions tab
  workflow_dispatch:

jobs:
  scrape-belgium:
    runs-on: ubuntu-latest
    
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'npm'

      - name: Install dependencies
        run: npm ci

      - name: Run Belgium scraper
        env:
          SUPABASE_URL: ${{ secrets.SUPABASE_URL }}
          SUPABASE_SERVICE_ROLE_KEY: ${{ secrets.SUPABASE_SERVICE_ROLE_KEY }}
          MISTRAL_API_KEY: ${{ secrets.MISTRAL_API_KEY }}
        run: |
          npx tsx server/run-scraper.ts

      - name: Notify on failure
        if: failure()
        run: echo "Belgium scraper failed. Check the logs above for details."

  scrape-uae:
    runs-on: ubuntu-latest
    needs: scrape-belgium  # Run after Belgium completes
    
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'npm'

      - name: Install dependencies
        run: npm ci

      - name: Run UAE scraper
        env:
          SUPABASE_URL: ${{ secrets.SUPABASE_URL }}
          SUPABASE_SERVICE_ROLE_KEY: ${{ secrets.SUPABASE_SERVICE_ROLE_KEY }}
          MISTRAL_API_KEY: ${{ secrets.MISTRAL_API_KEY }}
        run: |
          npx tsx server/uae/run-scraper.ts

      - name: Notify on failure
        if: failure()
        run: echo "UAE scraper failed. Check the logs above for details."
